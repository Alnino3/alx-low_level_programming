#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @l: the integer to be computed
 * Return: the absolute value
 */
int _abs(int l)
{
if (l >= 0)
return (l);
else
return (-l);
}
