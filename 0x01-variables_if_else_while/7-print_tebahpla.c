#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (cc = 'z'; cc >= 'a'; cc--)
	{
		putchar(cc);
	}
	putchar('\n');
	return (0);

}
